<!-- Thanks for contributing! Keep this short — a few honest lines beat a filled-in form. -->

## What does this change?

<!-- One or two sentences. What behaviour is different after this PR? -->

## Why?

<!-- The problem this solves. Link the issue: Closes #123 -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Prompt / commit-message-quality change
- [ ] Documentation
- [ ] Build, CI or tooling

## If this changes the prompt

<!-- Delete this section if it does not apply. -->

**Bad output observed (before):**

```
```

**Output after this change:**

```
```

## Checklist

- [ ] No third-party dependencies were added (standard library only)
- [ ] `commitclerk.py` is still a single self-contained file
- [ ] `python -m unittest discover -s tests` passes
- [ ] `python commitclerk.py --dry-run` was run against a real staged diff
- [ ] `README.md` updated if a flag, env var or default changed
- [ ] An entry was added to `CHANGELOG.md` under `## [Unreleased]`
